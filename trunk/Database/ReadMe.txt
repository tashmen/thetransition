The DB Creation Script will create the baseline database.  
There shouldn't be any user related records kept within this script.  
This script should only be used for creating the initial database as this will rebuild all of the tables and data.